ScanSnap Support Brain – full rebuild package

Enthält die verfügbaren Projektdateien mit korrigierter Ordnerstruktur.

Wichtig:
1. ZIP entpacken.
2. Inhalt in C:\Projects\scansnap-support-brain kopieren.
3. Vorhandene Dateien ersetzen.
4. Lokal testen:
   npm run dev
5. Erst wenn lokal alles läuft: GitHub Desktop Commit + Push.

Hinweis:
Diese ZIP enthält alle Dateien, die hier im Chat verfügbar waren. Falls dein lokales Projekt zusätzliche Ordner hat
(z. B. src/components, src/api, src/assets, src/lib/sessionStore.js, src/lib/uiTranslations.js, src/lib/localData.js),
diese bitte NICHT löschen, sondern behalten. Am besten die ZIP über den bestehenden Projektordner kopieren, nicht den Ordner vorher leeren.
